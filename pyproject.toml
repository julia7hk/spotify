[project]
name = "spotify-dashboard"
version = "0.1.0"
description = "Personal Spotify listening dashboard (Flask + Spotipy backend)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.2",
    "flask-cors>=6.0.2",
    "spotipy>=2.23.0",
    "python-dotenv>=1.2.2",
]
